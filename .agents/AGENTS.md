# Project Rules

- **Do NOT run `git commit` or `git push`**: The user will handle committing and pushing code at the end of the day. Only edit files and restart services/containers as needed. Do not make git commits unless explicitly asked by the user.
